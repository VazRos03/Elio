<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient"
    android:padding="16dp"
    tools:context=".PanelActivity">

    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/userIcon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/outline_account_circle_24"
            android:tint="@color/text_dark"
            android:contentDescription="Icono de Usuario" />

        <TextView
            android:id="@+id/userIdentifierTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:textSize="14sp"
            android:textColor="@color/text_dark"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            tools:text="usuario@ejemplo.com" />

        <Button
            android:id="@+id/logoutButton"
            android:layout_width="wrap_content"
            android:layout_height="42dp"
            android:text="Salir"
            android:paddingHorizontal="16dp"
            android:backgroundTint="@color/secondary_warm"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="bold"
            app:cornerRadius="8dp"
            android:elevation="4dp"/>
    </LinearLayout>

    <!-- 2. SECCIÓN DE FORMULARIO (Agregar Tareas) como Tarjeta -->
    <ScrollView
        android:id="@+id/formScrollView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:background="@drawable/rounded_card_background"
        android:elevation="8dp"
        app:layout_constraintTop_toBottomOf="@id/headerLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Nueva Tarea"
                android:textSize="20sp"
                android:textColor="@color/text_dark"
                android:fontFamily="sans-serif-bold"
                android:layout_marginBottom="16dp"/>

            <EditText
                android:id="@+id/titleEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Título corto"
                android:inputType="textCapSentences"
                android:padding="14dp"
                android:background="@drawable/rounded_edittext_soft"
                android:layout_marginBottom="12dp"
                android:textColor="@color/text_dark"
                android:textColorHint="@color/accent_light"/>

            <EditText
                android:id="@+id/dateEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Fecha de Entrega (DD/MM/AAAA)"
                android:inputType="date"
                android:padding="14dp"
                android:background="@drawable/rounded_edittext_soft"
                android:focusable="false"
                android:clickable="true"
                android:layout_marginBottom="12dp"
                android:textColor="@color/text_dark"
                android:textColorHint="@color/accent_light"/>

            <EditText
                android:id="@+id/descriptionEditText"
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:hint="Descripción detallada"
                android:inputType="textMultiLine|textCapSentences"
                android:gravity="top"
                android:padding="14dp"
                android:background="@drawable/rounded_edittext_soft"
                android:layout_marginBottom="20dp"
                android:textColor="@color/text_dark"
                android:textColorHint="@color/accent_light"/>

            <Button
                android:id="@+id/addTaskButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Agregar Tarea"
                android:paddingVertical="14dp"
                android:background="@drawable/rounded_button_gradient_primary"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:textStyle="bold"
                android:elevation="4dp"/>
        </LinearLayout>
    </ScrollView>


    <!-- 3. SECCIÓN DE LISTA DE TAREAS (RecyclerView) -->
    <TextView
        android:id="@+id/listTitleTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tareas Pendientes"
        android:textSize="20sp"
        android:textColor="@color/text_dark"
        android:fontFamily="sans-serif-bold"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/formScrollView"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/tasksRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/listTitleTextView"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:listitem="@layout/item_task"/>

</androidx.constraintlayout.widget.ConstraintLayout>